//-----------------------------------------------------------------------------------
#include "assets/tcn75/tcn75.c"
#include "assets/lcd/lcd_serial.c"
//-----------------------------------------------------------------------------------
float tempr;
uint8_t str[20], numStr[10];
//-----------------------------------------------------------------------------------
void setup()
{
  lcd_init();
  tcn75_init(32, 20, RES_11_BIT);  //High, Low, Resolution
  
  lcd_string(0, 5, "TCN75");
}
//-----------------------------------------------------------------------------------
void loop() 
{ 
  tempr = tcn75_getTemp(TEMP_REG);
  dtostrf(tempr, 7, 3, numStr);
  
  sprintf(str, "Temp: %s°C", numStr);
  lcd_string(1, 0, str);
}
//-----------------------------------------------------------------------------------
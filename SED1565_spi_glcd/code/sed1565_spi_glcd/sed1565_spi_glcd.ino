//------------------------------------------------------------------------------
#include "assets/tc72/tc72.c"
#include "assets/sed1565/sed1565.c"
//------------------------------------------------------------------------------
int16_t tempr;
uint8_t str[20], numStr[20];
//------------------------------------------------------------------------------
void setup() 
{
  sed1565_init();
  TC72_init();
  //sed1565_image(img);
  sed1565_printf(0, 20, "SED1565 SPI Glcd");
  sed1565_printf(2, 22, "TC72 Temp sensor");
}
//------------------------------------------------------------------------------
void loop() 
{  
  dtostrf(TC72_readTempr(), 6, 2, numStr);   // 6 chars min total width, 2 after decimal point
  sprintf(str, "Temp: %s°C", numStr, '%');  

  sed1565_printf(5, 0, str); 
}
//------------------------------------------------------------------------------